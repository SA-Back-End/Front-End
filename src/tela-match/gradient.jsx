import React from 'react';

const gradiente = () => {
    return (
        <div style={{
            width: '100%',
            height: '100%',
            background: 'linear-gradient(to bottom, #0052E0, #1F71FF)'
        }}>
        </div>
    );
};

export default gradiente;