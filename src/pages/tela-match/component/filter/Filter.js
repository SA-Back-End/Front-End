import './Filter.css'

export default function Filter() {   
    
    const filter = () {

    }
    
    return (
        <div>
            <select className="dropbox">
                <option>Área de atuação</option>
            </select>

            <select className="dropbox">
                <option>Nível de formação</option>
            </select>

            <select className="dropbox">
                <option>Status de disponibilidade</option>
            </select>

            <button className='btn-filter' onClick={}></button>
        </div>
    )
}

